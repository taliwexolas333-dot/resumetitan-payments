# ResumeTitan AI — Crypto Payment Configuration
# Copy this to .env and fill in your values

PORT=3000
SENTINEL_INTERVAL=60

# Optional: For real transaction monitoring, get a free API key
# BTC: Uses mempool.space (no key needed)
# POL: Uses polygonscan.com (free API key)
# SOL: Uses public Solana RPC (no key needed)

POLYGONSCAN_API_KEY=